@model IEnumerable<PresentationLayer.Models.StudentModel>

@{
    ViewBag.Title = "studentsbelow50";
}

<h2>studentsbelow50</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Studentid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ObtainedPercentage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CollegeId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Studentid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ObtainedPercentage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CollegeId)
        </td>
    
    </tr>
}

</table>
